# 개발경과01

## 1. 문서 목적

이 문서는 지금까지 `playready` 프로젝트를 어떤 순서로 개발했고, 중간에 어떤 문제가 있었고, 그 문제를 해결하려고 무엇을 바꾸었는지를 한 번에 이해하기 위한 기록입니다.

특히 이 문서는 아래 상황에 있는 사람을 기준으로 작성했습니다.

- 비개발자
- 바이브코딩 또는 AI 협업 개발이 처음인 사용자
- 프로젝트 기획자
- 프로젝트 관리자
- "지금 어디까지 왔고, 왜 여기서 모델 선택을 다시 고민하는지"를 빠르게 파악해야 하는 사람

쉽게 말하면, 이 문서는 `playready가 지금 왜 이 상태까지 왔는지`를 설명하는 **프로젝트 개발 일지 + 판단 근거 문서**입니다.

---

## 2. 프로젝트 목표

`playready`는 보드게임 룰북 자료 자동 생성 웹 서비스입니다.

사용자가 웹페이지에서 아래 정보를 입력하면:

- 게임 이름
- BGG(BoardGameGeek) 게임 ID
- 룰북 PDF
- 필요 시 추가 자료(PDF, 텍스트 등)

서비스가 자동으로 다음 두 문서를 한국어로 생성하는 것이 목표입니다.

### 문서 A
- 내가 게임을 공부하기 위한 상세 학습 자료
- 게임 목표, 구성물, 턴 순서, 핵심 메커니즘, 예외 규칙, FAQ, 전략 팁 포함

### 문서 B
- 다른 플레이어에게 게임을 설명할 때 쓰는 보조 자료
- 소개 스크립트, 셋업 체크리스트, 첫 라운드 진행 가이드, 헷갈리는 규칙 Q&A, 요약 카드, 용어집 포함

추가 목표:

- 한국어 용어 일관성 유지
- 가능하면 공식 한국어판 용어 우선 사용
- PDF 이미지도 설명 위치에 함께 삽입
- GitHub Pages + Cloudflare Workers + 별도 PDF 처리 서버 구조로 무료 또는 저비용 운영

---

## 3. 현재 시점 요약

현재까지 구현된 상태를 한 줄로 요약하면:

**서비스 뼈대와 전체 흐름은 거의 완성되었고, 실제 병목은 "어떤 AI 모델/과금 방식으로 최종 생성 단계를 운영할 것인가"에 모여 있습니다.**

즉, 지금은:

- 프론트엔드 화면 있음
- Cloudflare Worker 백엔드 있음
- Render에 PDF 추출 서버 배포됨
- BGG 수집 기능 있음
- 문서 생성 파이프라인 있음
- 다양한 예외 처리와 폴백 로직도 들어감

하지만 실제 최종 생성 단계에서:

- Gemini 무료 티어/쿼터 한도 문제
- 또는 실제 API 제한 문제

가 반복 확인되었고, 그래서 현재는 **모델 선택과 과금 구조 결정**이 핵심 이슈가 되어 있습니다.

---

## 4. 전체 개발 흐름 요약

지금까지의 개발은 아래 흐름으로 진행되었습니다.

1. 프로젝트 구조 설계
2. Cloudflare Worker 백엔드 구현
3. PDF 추출용 Python 서비스 구현
4. Render에 PDF 서버 배포
5. Cloudflare Worker 배포
6. 단일 HTML 프론트엔드 구현
7. GitHub Pages 배포
8. 실제 테스트 진행
9. 테스트 중 발견된 오류를 순차 수정
10. 최종적으로 AI 모델/과금 문제를 핵심 검토 포인트로 확인

---

## 5. 회차별 개발 경과

아래는 "어떤 문제가 있었고, 그래서 어떤 변경을 했는지"를 회차별로 정리한 내용입니다.

---

## 회차 1. 프로젝트 구조와 역할 분리 설계

### 당시 상황
프로젝트가 아직 비어 있었고, 어떤 폴더가 필요하고 어떤 기술을 어디에 둘지 먼저 정해야 했습니다.

### 결정한 구조
- 프론트엔드: 단일 HTML 기반, GitHub Pages 배포
- 백엔드: Cloudflare Workers
- PDF 처리: Python + PyMuPDF
- BGG 데이터 수집: Worker에서 XML API 호출
- AI 생성: Worker가 모델 API 호출

### 왜 이렇게 나눴나
Cloudflare Worker 안에서는 `PyMuPDF`를 직접 실행할 수 없기 때문입니다.

쉽게 말하면:
- Worker는 "접수 창구"
- Python 서버는 "PDF 분석실"

로 역할을 나눈 것입니다.

### 결과
초기 아키텍처가 확정되었고, 이후 개발 방향이 흔들리지 않게 되었습니다.

### 프로젝트 관리 관점 메모
- 초기에 구조를 분리한 판단은 적절했습니다.
- 이 결정 덕분에 이후 문제 발생 시 원인을 `프론트`, `Worker`, `PDF 서버`, `AI 모델`로 나눠 볼 수 있었습니다.

---

## 회차 2. Cloudflare Worker 백엔드 1차 구현

### 목표
사용자 입력을 받아서 전체 흐름을 지휘하는 백엔드를 만드는 단계였습니다.

### 구현한 것
- 게임 이름, BGG ID, PDF(base64) 수신
- BGG XML API 호출
- PDF 추출 서버 호출
- Gemini API 호출
- 문서 A/B 반환
- CORS 제한
- API 키를 secret/env로만 관리
- 파일 크기 제한 50MB
- 진행상황 스트리밍 엔드포인트 구현

### 처음 설계의 특징
처음에는 요구사항대로 `BGG -> PDF -> AI` 순서로 강하게 묶여 있었고,
중간 어느 단계라도 실패하면 전체가 실패하는 구조였습니다.

### 결과
서비스의 기본 뼈대는 만들어졌지만, 실제 운영 테스트를 하기 전이라 예외 상황 대응은 아직 약한 상태였습니다.

### 기술 참고
- Worker 엔드포인트:
  - `/api/health`
  - `/api/generate`
  - `/api/generate/stream`

---

## 회차 3. PDF 추출용 Python 서버 구현

### 목표
룰북 PDF에서 텍스트와 이미지를 함께 추출하는 기능을 만드는 단계였습니다.

### 구현한 것
- base64 PDF 수신
- 임시 파일 저장
- PyMuPDF로 텍스트 추출
- PyMuPDF로 이미지 추출
- 이미지 좌표 정보 수집
- 이미지와 가까운 텍스트 블록 연결
- 처리 후 임시 파일 삭제

### 왜 중요했나
프로젝트 요구사항 중 하나가:

> AI가 이미지를 분석하지 않고, PDF 안 원본 이미지를 그대로 추출해서 관련 설명 옆에 배치

였기 때문입니다.

### 결과
PDF 분석 기능은 구조적으로 완성되었습니다.

### 프로젝트 관리 관점 메모
- PDF 서버를 별도 서비스로 분리한 것은 좋은 선택이었습니다.
- 다만 이 구조는 이후 배포/운영 단계에서 "서버가 2개"라는 관리 포인트를 만들었습니다.

---

## 회차 4. Render 배포 이슈 해결

### 문제
Render에서 PDF 서버를 배포하려고 했을 때 아래 오류가 발생했습니다.

- `Root directory "pdf-processor" does not exist`

### 원인
GitHub 저장소에 `pdf-processor` 폴더가 아직 올라가지 않았거나,
Render 설정의 `Root Directory`가 실제 저장소 구조와 맞지 않았습니다.

### 조치
- 로컬 프로젝트를 git 저장소로 초기화
- GitHub 저장소와 연결
- 필요한 파일을 push
- Render의 Root Directory를 `pdf-processor`로 맞춤

### 결과
Render에 PDF 서버가 정상 배포되었고,
아래 health 체크가 성공했습니다.

- `https://playready.onrender.com/health`

### 의미
이 시점부터 PDF 처리 서버는 인터넷에서 실제 접근 가능한 상태가 되었습니다.

---

## 회차 5. Cloudflare Worker 배포 이슈 해결

### 문제 1
Cloudflare Worker 배포 중 이메일 인증 미완료로 배포 실패

### 원인
Cloudflare 계정의 이메일 인증이 안 되어 있었습니다.

### 조치
- Cloudflare 대시보드 접속
- 이메일 인증 완료
- `wrangler deploy` 재실행

### 문제 2
KV namespace 생성 시 "이미 존재함" 오류

### 원인
같은 이름의 KV가 이미 만들어져 있었습니다.

### 조치
- 기존 KV 목록 조회
- 해당 ID를 `wrangler.toml`에 연결

### 결과
Worker 배포 성공

정상 확인 주소:
- `https://playready-worker.iamsangmin.workers.dev/api/health`

### 의미
이 시점부터:
- Render PDF 서버
- Cloudflare Worker

둘 다 실서비스 주소로 살아 있게 되었습니다.

---

## 회차 6. 단일 HTML 프론트엔드 구현

### 목표
사용자가 직접 쓸 수 있는 화면을 만드는 단계였습니다.

### 구현한 것
- 하나의 `index.html` 파일 안에 CSS/JS 모두 포함
- 3개 화면 구성:
  - 입력 화면
  - 진행 화면
  - 결과 화면
- 모바일 반응형 디자인
- 탭 전환형 결과 보기
- HTML 다운로드 버튼
- 진행 메시지 표시
- Worker URL을 상단 설정 변수로 분리

### 디자인 방향
- 너무 딱딱하지 않은 보드게임 느낌
- 읽기 쉬운 레이아웃
- 모바일 친화적 구성

### 추가 조정
사용자가 PDF 없이도 진행할 수 있도록 Worker 입력 검증도 수정했습니다.

### 결과
사용자 입장에서 "페이지를 열고 입력하고 결과를 보는 흐름"이 완성되었습니다.

---

## 회차 7. GitHub Pages 배포

### 목표
단일 HTML 프론트엔드를 실제 웹페이지로 열 수 있게 배포하는 단계였습니다.

### 조치
- `index.html`을 저장소 루트에 배치
- GitHub Pages를 `main / root`로 설정

### 결과
사용자용 프론트엔드 배포 준비 완료

### 운영 관점 메모
- 프론트엔드는 정적 파일이라 관리가 쉬움
- 백엔드는 Worker/Render로 분리되어 있어 배포가 독립적임

---

## 회차 8. 테스트 1차: BGG 응답 실패

### 테스트 상황
샘플 게임 제목, BGG ID, PDF 룰북, FAQ 자료를 넣고 생성 테스트

### 발생한 문제
- `BGG 서버에서 응답이 없습니다. 잠시 후 다시 시도해주세요.`

### 원인 분석
초기 BGG 수집 로직은:
- 요청 간격이 2초로 짧은 편이었고
- 한 번 실패하면 전체 종료하는 구조였습니다.

즉, BGG가 느리거나 일부 스레드만 응답이 늦어도 전체 작업이 멈췄습니다.

### 해결 시도
- BGG 요청에 재시도 로직 추가
- 요청 간격을 5초로 증가
- 일부 포럼/스레드 실패 시 전체 중단 대신 건너뛰기
- `User-Agent` 추가
- BGG 전체 실패 시에도 PDF 중심으로 계속 진행하도록 완화

### 재테스트 결과
BGG 단계는 이전보다 더 진행되었고, 완전 중단 빈도가 줄었습니다.

### 관리 관점 분석
- 외부 서비스(BGG)는 내 서비스가 제어할 수 없는 변수
- 따라서 "실패 허용" 구조가 반드시 필요함
- 이 수정은 운영 안정성 측면에서 중요했습니다.

---

## 회차 9. 테스트 2차: PDF 파싱 실패

### 발생한 문제
- `PDF 파일을 읽을 수 없습니다. 파일이 손상되었거나 암호화되어 있을 수 있습니다.`

### 원인 분석
기존 PDF 서버는:
- 문서 열기
- 텍스트 추출
- 이미지 추출

이 중 하나라도 실패하면 전체를 실패로 처리했습니다.

하지만 실제 PDF는:
- 일부 페이지만 깨져 있거나
- 일부 이미지만 이상하거나
- 구조가 특이한 경우가 많습니다.

### 해결 시도
- base64 검증 강화
- 암호화 PDF 감지
- 일부 페이지 텍스트 추출 실패 시 건너뛰기
- 일부 이미지 추출 실패 시 건너뛰기
- 읽을 수 있는 부분만 계속 수집

### 재테스트 결과
PDF 처리 안정성이 개선되었고,
부분적으로 문제가 있는 PDF에서도 전체 실패 확률을 줄였습니다.

### 관리 관점 분석
- PDF는 사용자 입력 중 가장 품질 편차가 큰 자료
- "전부 성공"보다 "가능한 만큼 처리" 전략이 운영상 더 적절

---

## 회차 10. 테스트 3차: AI 단계 실패 (초기에는 입력 과다로 추정)

### 발생한 문제
- `AI 처리 중 오류가 발생했습니다.`

### 초기 추정
처음에는:
- PDF
- BGG
- 추가 자료

가 한꺼번에 Gemini로 들어가서 입력이 너무 큰 것으로 판단했습니다.

### 해결 시도 1
- BGG 요약량 줄이기
- PDF 텍스트 수 줄이기
- 이미지 수 줄이기
- 추가 자료 길이 줄이기

### 해결 시도 2
- `full -> minimal -> tiny` 자동 축소 재시도

### 해결 시도 3
- 한 번에 문서 A/B/용어집을 만들지 않고
- `용어집 -> 문서 A -> 문서 B`로 분리 생성

### 해결 시도 4
- BGG 제외
- 추가 자료 제외
- PDF 제외
- 최소 정보만으로도 생성 시도

### 당시 판단
처음에는 "Gemini 입력이 너무 크다" 쪽으로 문제를 보았습니다.

---

## 회차 11. 실제 원인 확인: Gemini 무료 티어 쿼터 초과

### 결정적 전환점
오류 메시지를 더 정확하게 보여주도록 바꾼 후, 실제 Gemini 응답 원인이 드러났습니다.

실제 메시지:

- `You exceeded your current quota`

### 진짜 원인
이 문제는 입력 과다만이 아니라,
실제로는 **Gemini 무료 사용량/쿼터 한도 초과**가 핵심 원인이었습니다.

즉:
- 코드 구조 문제도 일부 있었지만
- 가장 큰 병목은 모델 과금/쿼터 조건이었습니다.

### 해결 시도
- Gemini 오류 분류 로직 개선
- API 키 문제 / 모델 문제 / 쿼터 문제 / 권한 문제 / 실제 입력 초과 문제를 구분

### 결과
이제 서비스는 "왜 실패하는지"를 더 정확하게 보여줄 수 있게 되었습니다.

### 프로젝트 관리 관점 분석
- 이 시점에서 중요한 결론:

**현재 버전의 기술 구조는 대체로 맞지만, 무료 모델 운영 정책이 현재 목표 기능과 맞지 않는다.**

즉, 지금 남은 핵심 의사결정은:

- Gemini 유료 전환
- Gemini 모델 변경
- Claude 계열로 전환
- 생성 아키텍처를 더 쪼개기

중 무엇을 선택할지입니다.

---

## 6. 현재까지의 테스트 경과 정리

### 테스트 성공 항목
- Render PDF 서버 health 체크 성공
- Cloudflare Worker health 체크 성공
- 프론트엔드 접속 및 입력 화면 동작
- BGG 수집 단계 일부 정상 작동
- PDF 업로드/미업로드 양쪽 흐름 처리 가능

### 테스트 중 확인된 문제들
1. BGG 응답 지연/실패
2. PDF 파싱의 예외 상황
3. Gemini 무료 쿼터/사용량 한도 문제

### 이미 개선된 부분
1. BGG 재시도/부분 실패 허용
2. PDF 부분 실패 허용
3. AI 입력 축소
4. AI 호출 분리
5. 오류 메시지 세분화

---

## 7. 현재 남아 있는 핵심 이슈

현재 남아 있는 가장 중요한 이슈는 아래 하나로 정리됩니다.

### 핵심 이슈
**어떤 AI 모델과 어떤 과금 구조를 선택해야, 원래 원하는 기능을 유지하면서 안정적으로 운영할 수 있는가**

### 왜 이게 핵심인가
이제는 서비스 구조나 UI보다,
최종 생성 단계의 모델 선택이 전체 성공률과 비용을 결정하기 때문입니다.

---

## 8. 현재 고려 중인 선택지

### 선택지 A. Gemini 유료 전환

#### 장점
- 현재 코드 구조를 거의 유지 가능
- 변경량 적음
- 운영 전환이 가장 쉬움

#### 단점
- 어떤 Gemini 모델을 쓸지 선택해야 함
- 무료 티어보다 비용은 발생

#### 가장 현실적인 후보
- Gemini 2.5 Flash
- Gemini 2.5 Pro

---

### 선택지 B. Claude Haiku 4.5로 전환

#### 장점
- 유료 모델 기준으로 무료 쿼터 문제에서 벗어남
- 문장 품질이 안정적일 가능성 있음

#### 단점
- Anthropic API 방식으로 코드 수정 필요
- 지금 구조는 Gemini 중심이라 교체 비용이 있음

---

### 선택지 C. 상위 Claude 또는 상위 Gemini 모델 사용

#### 장점
- 품질 향상 기대
- 복잡한 문서 생성에 더 안정적일 수 있음

#### 단점
- 비용이 증가

---

## 9. 기획자/관리자 관점 분석

### 지금 프로젝트가 잘된 점
1. 구조 분리가 명확함
2. 문제 발생 지점을 빠르게 분리할 수 있음
3. 프론트/백엔드/PDF/AI 각 층이 분리되어 유지보수성이 좋음
4. 테스트 중 발견된 문제를 단계적으로 해결해 나감

### 지금 프로젝트의 리스크
1. 외부 서비스 의존도가 높음
   - BGG
   - Render
   - Cloudflare
   - AI API

2. AI 생성 품질과 비용이 모델 선택에 크게 좌우됨

3. 무료 티어 기반 서비스는 개발 중에는 유리하지만, 운영 단계에서는 불안정할 수 있음

### 운영 관점에서 가장 중요한 질문
1. 결과물 품질이 더 중요한가?
2. 비용이 더 중요한가?
3. 코드 변경량이 적은 것이 더 중요한가?

---

## 10. 현재 시점에서의 추천 해석

현재 상태를 기준으로 보면:

### 기능 유지 우선
원래 원하던 기능을 최대한 유지하고 싶다면,
가장 먼저 검토할 것은 **Gemini 유료 전환**입니다.

이유:
- 현재 구조를 가장 적게 바꾸고
- 이미 Worker 코드가 Gemini 호출에 맞춰져 있기 때문입니다.

### 품질 우선
문서 품질과 긴 규칙 설명 정리력을 가장 중요하게 본다면,
상위 Gemini 모델 또는 상위 Claude 모델이 더 적합할 수 있습니다.

### 비용+현실성 우선
월 사용 횟수가 많지 않다면,
유료 모델로 가더라도 총 비용은 의외로 작을 가능성이 큽니다.

---

## 11. 현재까지 수정된 핵심 방향 정리

지금까지의 수정 방향은 크게 보면 아래와 같습니다.

1. 전체 기능 구현
2. 실제 배포 가능 상태로 만들기
3. 외부 서비스 실패에 덜 민감하게 만들기
4. 사용자 입장에서 이해 가능한 오류 메시지 제공
5. 최종적으로 모델/과금 문제를 명확히 드러내기

즉, 지금은 "개발 실패 상태"가 아니라,
**프로토타입이 실제 운영 조건에 닿으면서 어느 지점이 병목인지 확인된 상태**라고 보는 것이 맞습니다.

---

## 12. 다음 의사결정 제안

현재 상태에서 가장 현실적인 다음 단계는 아래 중 하나입니다.

### 제안 1. Gemini 유료 모델로 계속 간다
- 현재 구조 유지
- 가장 적은 코드 변경
- 빠르게 서비스 검증 가능

### 제안 2. Claude 계열로 전환한다
- 모델 교체 비용은 있지만 대안 확보 가능
- Anthropic API에 맞춘 변경 필요

### 제안 3. 생성 구조를 더 세분화한다
- 비용은 낮출 수 있으나 개발 복잡도 증가
- "중간 요약 저장 -> 최종 문서 생성" 구조 필요

---

## 13. 비개발자를 위한 한 줄 결론

지금까지의 개발은 헛수고가 아닙니다.  
서비스는 대부분 완성되었고, 이제 남은 핵심은 **"어떤 유료 AI 모델을 선택해 실제 운영 수준으로 안정화할 것인가"** 입니다.

쉽게 말하면:

- 웹페이지는 있음
- 서버도 있음
- PDF도 읽을 수 있음
- BGG도 가져올 수 있음
- 문서 생성 로직도 있음

이제 남은 건 **AI 모델의 사용 조건과 비용을 정하는 단계**입니다.

---

## 14. 참고 파일

현재 프로젝트에서 중요한 파일:

- [`/C:/Users/kblife/Desktop/AI 실습/playready/index.html`](/C:/Users/kblife/Desktop/AI%20실습/playready/index.html)
- [`/C:/Users/kblife/Desktop/AI 실습/playready/worker/src/index.js`](/C:/Users/kblife/Desktop/AI%20실습/playready/worker/src/index.js)
- [`/C:/Users/kblife/Desktop/AI 실습/playready/worker/src/services/bgg/bggApi.js`](/C:/Users/kblife/Desktop/AI%20실습/playready/worker/src/services/bgg/bggApi.js)
- [`/C:/Users/kblife/Desktop/AI 실습/playready/worker/src/services/ai/geminiClient.js`](/C:/Users/kblife/Desktop/AI%20실습/playready/worker/src/services/ai/geminiClient.js)
- [`/C:/Users/kblife/Desktop/AI 실습/playready/worker/src/services/document/generationPipeline.js`](/C:/Users/kblife/Desktop/AI%20실습/playready/worker/src/services/document/generationPipeline.js)
- [`/C:/Users/kblife/Desktop/AI 실습/playready/worker/src/services/document/promptBuilder.js`](/C:/Users/kblife/Desktop/AI%20실습/playready/worker/src/services/document/promptBuilder.js)
- [`/C:/Users/kblife/Desktop/AI 실습/playready/pdf-processor/app/main.py`](/C:/Users/kblife/Desktop/AI%20실습/playready/pdf-processor/app/main.py)
- [`/C:/Users/kblife/Desktop/AI 실습/playready/docs/cloudflare-workers-deploy.md`](/C:/Users/kblife/Desktop/AI%20실습/playready/docs/cloudflare-workers-deploy.md)
- [`/C:/Users/kblife/Desktop/AI 실습/playready/docs/github-pages-deploy.md`](/C:/Users/kblife/Desktop/AI%20실습/playready/docs/github-pages-deploy.md)

---

## 15. 최종 메모

현재 프로젝트는 "무언가가 안 되어 멈춘 상태"가 아니라,
**실제 사용 수준까지 밀어붙여 본 결과, 무료 모델 사용 조건이 최종 병목이라는 사실을 확인한 상태**입니다.

이것은 오히려 중요한 진전입니다.

왜냐하면:
- 구조 문제인지
- 배포 문제인지
- PDF 문제인지
- BGG 문제인지
- AI 비용/쿼터 문제인지

를 지금은 꽤 명확히 구분할 수 있게 되었기 때문입니다.

다음 문서에서는 이 판단을 바탕으로:
- 어떤 모델을 선택할지
- 비용과 품질을 어떻게 맞출지
- 실제 운영 전략을 어떻게 가져갈지

를 이어서 정리하면 됩니다.

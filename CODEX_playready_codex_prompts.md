# playready — Codex 데스크탑 앱 단계별 프롬프트
> 프로젝트 폴더명 / GitHub 저장소명: `playready`

> 아래 프롬프트를 순서대로 Codex 데스크탑 앱 채팅창에 복사·붙여넣기 하세요.
> 각 단계가 완료되었다는 확인을 받은 후 다음 단계로 넘어가세요.

---

> ### Codex 데스크탑 앱 사용 시 알아두어야 할 점
>
> Codex 앱 자체는 OpenAI의 GPT 모델이 코딩 작업을 수행합니다.
> 단, Codex가 만들어주는 playready 서비스 안에서 실제 룰북을 분석하고
> 문서를 생성하는 AI는 우리가 선택한 Gemini Flash 또는 Claude Haiku입니다.
> 즉, "Codex = 공사를 해주는 건설사", "Gemini/Claude = 실제 입주해서 일하는 직원"
> 으로 이해하면 됩니다.

---

## STEP 1 — 프로젝트 구조 설계 및 보안 환경 세팅

```
안녕. 지금부터 보드게임 룰북 자료 자동 생성 웹 서비스 "playready"를
단계적으로 만들 거야.
전체 내용을 설명할 테니, 지금 이 단계에서는 실제 코드 작성은 하지 말고
프로젝트 구조와 진행 계획만 먼저 정리해줘.

[서비스 개요]
사용자가 웹 페이지에서 보드게임 이름, BGG(boardgamegeek.com) 게임 ID,
룰북 PDF 파일을 입력하면, 아래 두 가지 한국어 문서를 자동 생성하는 서비스야.

- 문서 A: 내가 게임을 공부하기 위한 상세 학습 자료
  (게임 목표, 구성물, 턴 순서, 핵심 메커니즘, 예외 규칙, BGG 유저 FAQ, 전략 팁 포함)

- 문서 B: 다른 플레이어에게 게임을 설명할 때 사용하는 보조 자료
  (소개 스크립트, 셋업 체크리스트, 첫 라운드 진행 가이드, 자주 헷갈리는 규칙 Q&A,
   요약 참조 카드, 용어집 포함)

[기술 구성]
1. 프론트엔드: HTML 단일 파일 (GitHub Pages로 무료 배포, 모바일에서도 접속 가능)
2. 백엔드: Cloudflare Workers (무료, API 키를 안전하게 보관하는 중간 서버)
3. AI 모델: Google Gemini Flash (무료 티어, 추후 Claude Haiku로 교체 가능한 구조)
4. PDF 처리: PyMuPDF (룰북에서 텍스트와 이미지를 함께 추출)
5. BGG 데이터: boardgamegeek.com XML API 활용 (포럼 Q&A, FAQ 수집)

[이미지 처리 방식]
AI가 이미지를 분석하지 않고, PDF에서 이미지를 원본 그대로 추출해서
관련 텍스트 설명 옆에 자동 삽입. (추가 비용 없음)

[번역 및 용어 규칙 - 매우 중요]
- 모든 출력 문서는 반드시 한국어 공식 표현으로 작성
- 게임 공식 한국어판이 존재하면 해당 용어를 최우선 적용
- 공식 한국어판이 없으면 boardlife.co.kr 등 국내 보드게임 커뮤니티의
  통용 표현을 사용
- 같은 용어가 문서 전체에서 반드시 동일하게 표기되어야 함
- 번역 결과물은 반드시 용어 일관성 자동 검증 과정을 거침

[보안 요구사항 - 절대 위반 금지]
- API 키는 절대로 HTML 파일이나 GitHub에 올라가는 파일에 포함하지 않음
- API 키는 Cloudflare Workers 환경변수에만 저장
- 사용자가 업로드하는 PDF 파일은 처리 후 즉시 삭제, 서버에 저장하지 않음
- BGG 스크래핑 시 해당 사이트의 이용약관을 준수하고 요청 간격을 적절히 둠

[폴더 구조 요청]
위 내용을 바탕으로 아래를 만들어줘.
1. 프로젝트 전체 폴더 및 파일 구조 (트리 형태로)
2. 각 파일이 어떤 역할을 하는지 한 줄 설명
3. 진행 순서 (총 몇 단계로 진행할 건지)
4. 내가 직접 해야 하는 작업 목록 (계정 생성, 키 발급 등)
   → 각 항목마다 어떤 사이트에서, 어떻게 하면 되는지 구체적으로 설명해줘

코드 작성은 아직 하지 말고, 위 계획과 구조만 먼저 보여줘.
내가 확인하고 승인하면 다음 단계로 넘어갈게.
```

---

## STEP 2 — Cloudflare Workers 백엔드 구축 (보안 서버)

> ⚠️ STEP 1에서 Codex가 알려준 "사전 준비 목록"을 먼저 완료한 후 진행하세요.
> (Cloudflare 계정 생성, Google AI Studio API 키 발급 등)

```
STEP 1 계획을 확인했어. 이제 STEP 2를 진행해줘.

Cloudflare Workers 백엔드를 만들어줘.
아래 기능을 모두 포함해야 해.

[백엔드가 처리해야 하는 작업 순서]
1. 프론트엔드에서 전달받은 게임 이름, BGG ID, PDF 파일(base64 인코딩)을 수신
2. BGG XML API로 해당 게임의 포럼 글, FAQ, Q&A를 수집
   - BGG API 엔드포인트: https://boardgamegeek.com/xmlapi2/
   - 포럼 목록 → 각 스레드 → 댓글 순으로 수집
   - 요청 사이가 최소 2초 간격 (BGG 서버 과부하 방지)
   - 영어, 독일어, 프랑스어, 스페인어, 한국어, 일본어 게시물 모두 수집
3. PDF에서 텍스트와 이미지를 추출 (PyMuPDF 사용)
   - 텍스트: 섹션 구조 유지하며 추출
   - 이미지: 각 이미지가 몇 페이지 어느 위치에 있는지 좌표 정보와 함께 추출
   - 추출된 이미지는 base64로 인코딩해서 텍스트와 연결
4. Gemini Flash API를 호출해서 아래 두 문서를 생성
   - 입력: 추출된 텍스트 + BGG 포럼 데이터 + 아래 번역/용어 규칙
   - 출력: 문서 A (학습용), 문서 B (설명용)
5. 생성된 문서에서 이미지 삽입 위치를 자동으로 결정해서 원본 이미지를 배치
6. 처리 완료 후 모든 임시 데이터 즉시 삭제
7. 완성된 두 문서를 HTML 형태로 프론트엔드에 반환

[Gemini API 호출 시 반드시 포함할 시스템 프롬프트]
아래 내용을 Gemini 시스템 프롬프트로 사용해줘.

"""
당신은 보드게임 룰북 분석 전문가입니다.
모든 출력은 반드시 한국어 공식 표현으로 작성합니다.

[용어 적용 우선순위]
1순위: 게임 공식 한국어판에서 사용하는 용어
2순위: boardlife.co.kr, divedice.com 등 국내 주요 보드게임 커뮤니티의 통용 표현
3순위: 영어 원어를 한국어로 직역한 표현 (이 경우 괄호 안에 원어 병기)

[일관성 규칙]
- 동일한 게임 용어는 문서 전체에서 반드시 같은 표기를 사용합니다.
- 문서 생성 전 반드시 용어집을 먼저 확정하고, 그 용어집을 기반으로 문서를 작성합니다.
- 같은 개념을 여러 언어로 설명한 자료가 있으면 통합하여 중복 없이 정리합니다.

[정확성 규칙]
- 룰북 원문에 없는 내용은 추가하지 않습니다.
- BGG 포럼 내용은 "커뮤니티 의견" 또는 "디자이너 공식 답변"으로 명확히 구분합니다.
- 불확실한 내용은 반드시 "확인 필요" 표시를 합니다.
"""

[보안 요구사항]
- Gemini API 키는 반드시 Cloudflare Workers 환경변수(wrangler secret)로만 관리
- 코드 어디에도 API 키를 직접 작성하지 않음
- CORS 설정: 내 GitHub Pages 도메인에서만 요청을 허용
- 요청 크기 제한: PDF 파일 최대 50MB
- 처리 시간 제한: 최대 10분 (Cloudflare Workers 무료 한도 고려)

[에러 처리]
- BGG API 응답 없음: "BGG 서버에서 응답이 없습니다. 잠시 후 다시 시도해주세요." 반환
- PDF 파싱 실패: "PDF 파일을 읽을 수 없습니다. 파일이 손상되었거나 암호화되어 있을 수 있습니다." 반환
- Gemini API 오류: 자동으로 1회 재시도 후 실패 시 "AI 처리 중 오류가 발생했습니다." 반환
- 모든 에러는 사용자가 이해할 수 있는 한국어 메시지로 반환

[진행 상황 알림]
작업이 완료되는 중간중간 진행 상황을 프론트엔드에 전달해줘.
- "BGG 포럼 데이터를 수집하고 있습니다... (1/3)"
- "PDF를 분석하고 있습니다... (2/3)"
- "한국어 문서를 생성하고 있습니다... (3/3)"

코드 작성이 끝나면 내가 Cloudflare에 배포하는 방법을
단계별로 스크린샷처럼 아주 자세하게 설명해줘.
(어떤 사이트에 접속해서, 어떤 버튼을 누르고, 어디에 무엇을 입력하는지까지)
```

---

## STEP 3 — 프론트엔드 HTML 페이지 제작

> ⚠️ STEP 2 배포가 완료되고 Cloudflare Workers URL을 받은 후 진행하세요.

```
STEP 2 백엔드 배포가 완료됐어. Workers URL은 [여기에 붙여넣기] 야.

이제 사용자가 사용할 HTML 페이지를 만들어줘.
단일 HTML 파일 하나로 모든 것을 구현해줘 (CSS, JavaScript 모두 한 파일에).

[페이지 구성 - 총 3개 화면, 하나의 HTML 파일에서 전환]

-- 화면 1: 입력 화면 --
아래 입력 항목을 포함해줘.
- 게임 이름 입력란 (한국어 이름 + 영어 원제, 예: "아크노바 (Ark Nova)")
- BGG 게임 ID 입력란
  → 입력란 아래에 작은 안내문 추가:
    "BGG ID는 boardgamegeek.com에서 게임을 검색한 후,
     주소창의 숫자입니다. 예: boardgamegeek.com/boardgame/342942/ark-nova 에서 342942가 ID입니다."
- 룰북 PDF 업로드 버튼 (선택사항, 없어도 진행 가능)
  → 업로드 버튼 아래 안내문: "PDF가 없어도 BGG 데이터만으로 문서를 생성할 수 있습니다.
     PDF가 있으면 더 상세한 문서가 만들어집니다."
- 추가 자료 업로드 버튼 (선택사항, 플레이어 가이드 등)
- "문서 생성 시작" 버튼

-- 화면 2: 진행 상황 화면 --
- 현재 진행 단계 표시 (BGG 수집 중 → PDF 분석 중 → 문서 생성 중)
- 단계별 진행 바
- 예상 남은 시간 표시
- 취소 버튼

-- 화면 3: 결과 화면 --
- 문서 A와 문서 B를 탭으로 전환하며 미리보기
- 각 문서 안에 룰북 원본 이미지가 관련 텍스트 옆에 삽입된 형태로 표시
- 버튼: "문서 A 다운로드 (HTML)", "문서 B 다운로드 (HTML)"
- 버튼: "처음으로 돌아가기"

[디자인 요구사항]
- 모바일 화면(스마트폰)에서도 편하게 사용할 수 있도록 반응형으로 만들어줘
- 깔끔하고 읽기 쉬운 디자인
- 보드게임 느낌이 나는 색감과 폰트 (너무 딱딱하지 않게)
- 로딩 중일 때 사용자가 지루하지 않도록 재미있는 문구 표시
  예: "BGG 포럼의 열띤 토론을 읽고 있습니다...", "규칙서를 꼼꼼히 번역하고 있습니다..."

[기술 요구사항]
- 외부 라이브러리 최소화 (CDN으로 불러오는 것은 허용)
- PDF 파일은 백엔드로 전송 전에 50MB 초과 여부 확인, 초과 시 안내 메시지 표시
- Cloudflare Workers URL은 코드 상단의 설정 변수로 관리
  (나중에 내가 쉽게 바꿀 수 있도록)
- 백엔드에서 에러 메시지를 받으면 사용자에게 친절한 한국어로 표시

완성된 HTML 파일은 GitHub Pages에 올리는 방법도 단계별로 설명해줘.
(GitHub 계정이 있다고 가정하고, repository 생성부터 Pages 활성화까지)
```

---

## STEP 4 — Codex Skills 등록 (재사용 환경 구성)

> ⚠️ STEP 3까지 완료되어 웹 서비스가 정상 작동하는 것을 확인한 후 진행하세요.
>
> Codex 데스크탑 앱의 Skills는 파일로 만드는 것이 아니라
> Codex 앱 안의 Skills 패널에서 직접 등록합니다.
> 이 단계에서 Codex가 각 Skill의 내용을 작성해주면,
> 내가 복사해서 Codex 앱의 Skills 패널에 붙여넣으면 됩니다.

```
STEP 3까지 완료돼서 웹 서비스가 잘 작동하고 있어.

이제 Codex 앱의 Skills 패널에 등록할 내용들을 만들어줘.
나중에 새 게임이 생길 때마다 Codex 채팅창에
"/playready 게임명" 한 줄만 입력해도 전체 작업이 자동 실행되게 하고 싶어.

[만들어야 할 것 1 - Skills 패널에 등록할 Skill 4개]
각 Skill의 이름과 내용(어떤 작업을 하는지 설명하는 텍스트)을 작성해줘.
내가 Codex 앱의 Skills 패널에서 "New Skill" 버튼을 눌러서 직접 붙여넣을 거야.

- Skill 1: playready-bgg-collector
  BGG 포럼 데이터 수집 방법, 요청 규칙, 수집할 언어 범위 정의

- Skill 2: playready-pdf-extractor
  PDF에서 텍스트+이미지를 추출하는 방법, 이미지 배치 규칙 정의

- Skill 3: playready-korean-translator
  다국어 → 한국어 번역 규칙, 용어 우선순위, 일관성 유지 방법 정의

- Skill 4: playready-doc-generator
  문서 A와 문서 B의 구조, 각 섹션 내용, 출력 형식 정의

[만들어야 할 것 2 - 메인 Skill (오케스트레이터)]
위 4개 Skill을 순서대로 호출하는 메인 Skill을 만들어줘.

- Skill 이름: playready
- 트리거 명령어: /playready
- 동작: 게임명과 BGG ID를 입력받아 위 4개 Skill을 순서대로 실행하고
  문서 A, B를 생성하는 전체 파이프라인

[만들어야 할 것 3 - Automations 설정 안내]
Codex 앱의 Automations 기능을 활용해서
매주 월요일 아침에 등록된 게임들의 BGG 포럼을 자동 확인하고
새로운 Q&A나 룰 업데이트가 있으면 알려주는 자동화를 설정하고 싶어.
Codex Automations 패널에서 어떻게 설정하면 되는지 단계별로 설명해줘.

[만들어야 할 것 4 - 모델 변경 설정 파일]
현재 사용 모델(Gemini Flash)이 어떤 파일의 어디에 설정되어 있는지
한 눈에 볼 수 있는 설정 파일을 따로 만들어줘.
- 파일 이름: config.json
- 나중에 내가 Codex 채팅창에 "모델을 Claude Haiku로 바꿔줘"라고 말하면
  이 파일 하나만 수정하면 모든 곳에 반영되는 구조로 만들어줘

모든 내용이 준비되면, 앞으로 새 게임을 추가할 때
정확히 어떤 순서로 무엇을 하면 되는지
초보자용 사용 설명서를 간단히 작성해줘.
```

---

## STEP 5 — 전체 테스트 및 최종 점검

> ⚠️ STEP 4까지 완료 후 진행하세요.

```
모든 세팅이 완료됐어. 이제 전체 테스트를 진행해줘.

[테스트 방법]
아래 게임으로 전체 파이프라인을 테스트해줘.
- 게임명: 카르카손 (Carcassonne)
- BGG ID: 822
- PDF: 없이 BGG 데이터만으로 테스트

[확인해야 할 항목들]
1. BGG 데이터 수집이 정상적으로 되는가
2. 한국어 번역 품질이 적절한가 (용어가 일관되게 사용되는가)
3. 문서 A와 B의 구조가 요청한 대로 생성되는가
4. HTML 페이지에서 결과물이 정상적으로 표시되는가
5. 모바일 화면에서도 잘 보이는가
6. 다운로드 버튼이 정상 작동하는가
7. 보안: API 키가 어디에도 노출되지 않는가

[테스트 결과 보고]
각 항목의 테스트 결과를 "정상 / 수정 필요 / 확인 불가" 로 정리해서 보여줘.
수정이 필요한 항목은 즉시 수정해줘.

[최종 체크리스트]
모든 테스트가 완료되면 아래를 최종 확인해줘.
1. GitHub Pages URL이 정상 접속되는가
2. Cloudflare Workers가 정상 작동하는가
3. 환경변수(API 키)가 안전하게 저장되어 있는가
4. 나중에 모델 변경 시 수정해야 할 파일과 위치가 명확한가
5. 백업 방법: 현재 상태를 Git commit으로 저장해줘
   (나중에 문제 생기면 이 시점으로 되돌아올 수 있도록)

테스트 완료 후, 내가 앞으로 이 서비스를 사용하는 방법을
딱 한 페이지 분량의 요약본으로 만들어줘.
```

---

## STEP 6 — (선택) Claude Haiku로 모델 전환

> ⚠️ Gemini Flash 결과물 품질에 불만족스러울 때만 진행하세요.
> 사전 준비: console.anthropic.com 에서 Anthropic 계정 생성 및 API 키 발급 필요

```
Gemini Flash 결과물을 확인해봤는데 Claude Haiku로 바꾸고 싶어.

[준비한 것]
- Anthropic API 키: [여기에 붙여넣기, 단 이 채팅 외부에 보관할 것]

[요청사항]
1. config.json에서 모델 설정을 Gemini Flash에서 Claude Haiku로 변경해줘
   - 변경되는 파일과 정확한 위치를 모두 알려줘
   - 모델명: claude-haiku-4-5-20251001
2. Cloudflare Workers 환경변수에서
   GEMINI_API_KEY를 제거하고 ANTHROPIC_API_KEY를 추가하는 방법 알려줘
   (어떤 사이트에서 어떻게 하는지 단계별로 설명)
3. Claude와 Gemini는 API 호출 방식이 약간 달라서 코드 수정이 필요할 수 있어
   필요한 코드 수정을 모두 자동으로 처리해줘
4. 변경 후 카르카손으로 다시 테스트해서
   Gemini 결과물과 비교 요약을 보여줘

변경 전에 현재 상태를 Git commit으로 저장해줘.
(문제가 생기면 "Gemini 버전으로 되돌려줘"라고 말하면 복구할 수 있도록)
```

---

## 참고: 문제 발생 시 사용할 프롬프트

```
[오류 발생 시]
방금 [어떤 단계]를 진행하다가 아래 오류가 발생했어.
[오류 메시지를 그대로 붙여넣기]
이 오류가 왜 발생하는지 쉽게 설명해주고, 해결 방법을 단계별로 알려줘.
IT 비전문가도 이해할 수 있는 수준으로 설명해줘.
```

```
[이전 상태로 되돌리고 싶을 때]
지금 변경한 내용에 문제가 있어. 직전 Git commit 상태로 전부 되돌려줘.
어떤 파일들이 복구되는지 목록으로 보여줘.
```

```
[새 게임 자료 생성 시 - 세팅 완료 후 매번 사용]
/playready
게임명: [한국어 이름 (영어 원제)]
BGG ID: [숫자]
PDF: [파일 첨부 또는 "없음"]
```
